server:
  port: 8099

eureka:
  instance:
    hostname: docker-desktop
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://127.0.0.1:${server.port}/eureka/
      