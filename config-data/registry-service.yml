server:
  port: 8099

eureka:
  instance:
    hostname: registro
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://registro:${server.port}/eureka/
      