server:
  port : 8099

eureka:
  instance:
    hostname: registry-service1.azurewebsites.net
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}/eureka/
      