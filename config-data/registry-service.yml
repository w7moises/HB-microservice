server:
  port: 8099

eureka:
  instance:
    hostname: docker-desktop
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://192.168.1.8:${server.port}/eureka/
      